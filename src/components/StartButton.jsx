import React from "react";

const StartButton = () => {
  return (
    <button className="mx-2 bg-orange-300 rounded-full h-10 w-10 flex items-center justify-center">
      {/* Aquí puedes poner el icono del botón de inicio */}
    </button>
  );
};

export default StartButton;
